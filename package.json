{
  "name": "gifty",
  "version": "1.0.0",
  "private": true,
  "description": "Gifty — crea e condividi liste regali con budget. App React + Firebase.",
  "homepage": ".",
  "dependencies": {
    "firebase": "^3.8.0",
    "prop-types": "^15.5.10",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-redux": "^5.0.4",
    "react-router-dom": "^4.1.1",
    "redux": "^3.6.0",
    "redux-thunk": "^2.2.0"
  },
  "devDependencies": {
    "firebase-tools": "^3.8.0",
    "prettier": "^1.4.0",
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "format": "prettier --single-quote --write \"src/**/*.js\"",
    "deploy": "npm run build && firebase deploy"
  },
  "eslintConfig": {
    "extends": "react-app"
  }
}
