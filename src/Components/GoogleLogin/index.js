import React from 'react';
import './googleLogin.css';

const GoogleLogin = () => (
  <div className="login__container">
    <h3>Accedi con Google</h3>
    <p>Accedi per ritrovare le tue liste su ogni dispositivo.</p>
    <button className="login__button">Accedi con Google</button>
  </div>
);

export default GoogleLogin;
